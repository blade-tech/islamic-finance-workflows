# ============================================================================
# NETLIFY CONFIGURATION - NEXT.JS DEPLOYMENT
# ============================================================================
# Islamic Finance Workflows - Next.js 14 App Router Demo
# Uses Netlify Next.js plugin for deployment

[build]
  command = "./node_modules/.bin/next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
