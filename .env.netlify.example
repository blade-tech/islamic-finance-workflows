# ============================================================================
# NETLIFY ENVIRONMENT VARIABLES - FRONTEND-ONLY DEPLOYMENT
# ============================================================================
# Islamic Finance Workflows - Demo Configuration
#
# SETUP INSTRUCTIONS:
# 1. Go to Netlify Dashboard → Site Settings → Environment Variables
# 2. Add these variables (NO backend URL needed - frontend uses mock data)
# 3. Redeploy after adding variables

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================

# Next.js Production Mode
NODE_ENV=production

# Port configuration (Netlify handles this automatically)
# PORT is set by Netlify - no need to configure

# ============================================================================
# MOCK DATA MODE (DEMO ONLY)
# ============================================================================
# The frontend uses hardcoded mock data for the demo
# No API calls are made - no backend required
# This environment variable can be used to toggle mock mode if needed

NEXT_PUBLIC_USE_MOCK_DATA=true
NEXT_PUBLIC_API_URL=mock://localhost

# ============================================================================
# OPTIONAL: Future Backend Integration
# ============================================================================
# If you later decide to add a real backend (Render, Railway, etc.):
# 1. Uncomment and set NEXT_PUBLIC_API_URL to your backend URL
# 2. Remove the mock data logic from the frontend
# 3. Redeploy

# NEXT_PUBLIC_API_URL=https://your-backend.render.com

# ============================================================================
# NOTES
# ============================================================================
# - No API keys needed (no Claude, Neo4j, OpenAI calls)
# - No database credentials needed (mock data in memory)
# - No CORS configuration needed (no backend)
# - Deployment is completely free (Netlify free tier)
# - Cold starts: None (static frontend)
# - Response time: Instant (no API calls)
